/*
  Nothing fancy, just the URL for the API.  It's here so I can git ignore it but then 
  I forgot so it'll just live here forever.

*/
const venueApi = (endpoint) => {
  return 'http://venue-api-jaime-barriga.herokuapp.com/' + endpoint;
};

export default venueApi;